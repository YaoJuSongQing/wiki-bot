# Wiki Q&A Bot 配置（模板）
# 复制为 config.yaml 并填自己的 API key

wiki:
  name: "LLM CLI 文档"
  url: "https://llm.datasette.io"
  type: "sphinx"

llm:
  provider: "deepseek"
  api_key: "sk-这里填你的key"
  model: "deepseek-chat"
  base_url: "https://api.deepseek.com/v1"

server:
  host: "0.0.0.0"
  port: 8080
